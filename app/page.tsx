'use client'

import { useState } from 'react'
import {
  Activity, ArrowUpRight, BarChart3, BookOpen, Check, ChevronDown, Clipboard,
  Code2, CreditCard, Copy, KeyRound, LayoutDashboard, LifeBuoy, Menu, Moon,
  MoreHorizontal, Plus, RefreshCw, Search, Settings, ShieldCheck, Sparkles,
  Sun, Terminal, UserRound, Wallet, X, Zap,
} from 'lucide-react'

const nav = [
  { label: 'Overview', icon: LayoutDashboard },
  { label: 'API Keys', icon: KeyRound },
  { label: 'Billing & Plans', icon: CreditCard },
  { label: 'Documentation', icon: BookOpen },
]
const chart = [32, 46, 40, 58, 48, 68, 63, 78, 69, 84, 75, 92, 88, 96, 81, 90, 77, 86, 72, 80, 70, 76, 62, 72, 61, 68, 56, 64, 52, 60]

function Logo() { return <div className="flex items-center gap-2.5"><div className="grid size-8 place-items-center rounded-xl bg-primary text-primary-foreground shadow-sm"><Activity size={17} strokeWidth={2.5} /></div><span className="font-semibold tracking-tight">TransVoice <span className="text-primary">AI</span></span></div> }
function Badge({ children, tone = 'green' }: { children: React.ReactNode; tone?: 'green'|'blue'|'gray' }) { return <span className={`badge badge-${tone}`}>{children}</span> }
function Stat({ title, value, change, icon: Icon }: any) { return <div className="panel p-5"><div className="flex items-start justify-between"><div><p className="eyebrow">{title}</p><p className="mt-2 text-2xl font-semibold tracking-tight">{value}</p></div><div className="icon-box"><Icon size={17} /></div></div><p className="mt-4 text-xs text-muted-foreground"><span className="font-medium text-emerald-600">{change}</span> from last month</p></div> }
function Chart() { return <div className="panel p-5"><div className="flex items-start justify-between"><div><p className="eyebrow">API requests</p><p className="mt-1 text-2xl font-semibold tracking-tight">1,284,392</p></div><select className="select" defaultValue="30"><option value="30">Last 30 days</option><option value="7">Last 7 days</option></select></div><div className="mt-7 h-48 w-full"><svg viewBox="0 0 800 190" preserveAspectRatio="none" className="h-full w-full" role="img" aria-label="API requests over the last 30 days"><defs><linearGradient id="fill" x1="0" x2="0" y1="0" y2="1"><stop offset="0" stopColor="var(--primary)" stopOpacity=".22"/><stop offset="1" stopColor="var(--primary)" stopOpacity="0"/></linearGradient></defs><path d={`M 0 190 L ${chart.map((v,i)=>`${i*27.5} ${190-v*1.55}`).join(' L ')} L 800 190 Z`} fill="url(#fill)"/><path d={`M ${chart.map((v,i)=>`${i*27.5} ${190-v*1.55}`).join(' L ')}`} fill="none" stroke="var(--primary)" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round"/></svg></div><div className="mt-3 flex items-center gap-5 text-xs text-muted-foreground"><span className="flex items-center gap-2"><i className="legend-dot bg-primary"/>Successful <b className="font-medium text-foreground">1,278,641</b></span><span className="flex items-center gap-2"><i className="legend-dot bg-rose-400"/>Failed <b className="font-medium text-foreground">5,751</b></span></div></div> }
function Overview() { return <div className="space-y-5"><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Stat title="Total requests" value="1.28M" change="12.4%" icon={Zap}/><Stat title="Characters processed" value="48.6M" change="8.2%" icon={BarChart3}/><Stat title="Avg. latency" value="184ms" change="4.1%" icon={Activity}/><Stat title="Success rate" value="99.55%" change="0.8%" icon={ShieldCheck}/></div><div className="grid gap-5 xl:grid-cols-[1.6fr_1fr]"><Chart/><div className="panel p-5"><div className="flex items-center justify-between"><div><p className="eyebrow">Monthly quota</p><p className="mt-1 text-2xl font-semibold tracking-tight">48.6M <span className="text-sm font-normal text-muted-foreground">/ 100M chars</span></p></div><Badge>Pro plan</Badge></div><div className="mt-6 h-2 rounded-full bg-muted"><div className="h-2 w-[49%] rounded-full bg-primary"/></div><div className="mt-3 flex justify-between text-xs text-muted-foreground"><span>49% used</span><span>Resets in 12 days</span></div><div className="mt-8 divide-y divide-border"><div className="flex items-center justify-between py-3 text-sm"><span className="text-muted-foreground">Speech to text</span><span className="font-medium">32.4M chars</span></div><div className="flex items-center justify-between py-3 text-sm"><span className="text-muted-foreground">Text to speech</span><span className="font-medium">16.2M chars</span></div></div><button className="button button-secondary mt-5 w-full">Manage usage <ArrowUpRight size={15}/></button></div></div><div className="panel overflow-hidden"><div className="flex items-center justify-between border-b border-border px-5 py-4"><div><p className="font-medium">Recent activity</p><p className="text-xs text-muted-foreground">Latest API events</p></div><button className="text-xs font-medium text-primary">View all</button></div>{['Speech transcription completed','API key created','Monthly quota updated'].map((x,i)=><div className="flex items-center gap-3 border-b border-border px-5 py-4 last:border-0" key={x}><div className="icon-box size-8"><Check size={14}/></div><div className="flex-1"><p className="text-sm font-medium">{x}</p><p className="text-xs text-muted-foreground">{i === 0 ? '2 minutes ago' : i === 1 ? 'Yesterday at 4:32 PM' : 'May 28, 2024'}</p></div><Badge tone="gray">{i === 0 ? '200 OK' : 'System'}</Badge></div>)}</div></div> }
function Keys() { const [keys,setKeys]=useState(['tv_live_••••••••••••••••••••••••••••••••']); const [copied,setCopied]=useState(false); const add=()=>setKeys([...keys, 'tv_test_••••••••••••••••••••••••••••••••']); return <div className="space-y-5"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="eyebrow">Developer access</p><h2 className="section-title">API keys</h2><p className="mt-1 text-sm text-muted-foreground">Manage credentials for your TransVoice AI projects.</p></div><button className="button button-primary" onClick={add}><Plus size={16}/> Create new key</button></div><div className="panel p-5"><div className="flex gap-3 rounded-xl border border-primary/20 bg-primary/5 p-4 text-sm"><ShieldCheck size={18} className="mt-0.5 shrink-0 text-primary"/><p><b>Keep your keys secure.</b> Never share them in client-side code or commit them to source control.</p></div><div className="mt-6 space-y-3">{keys.map((key,i)=><div className="flex flex-col gap-3 rounded-xl border border-border p-4 sm:flex-row sm:items-center" key={i}><div className="grid size-9 place-items-center rounded-lg bg-muted"><KeyRound size={16}/></div><div className="min-w-0 flex-1"><p className="text-sm font-medium">{i ? 'Test key' : 'Production key'} <Badge tone={i ? 'blue' : 'green'}>{i ? 'Test' : 'Active'}</Badge></p><p className="mt-1 truncate font-mono text-xs text-muted-foreground">{key}</p><p className="mt-1 text-xs text-muted-foreground">Created {i ? 'just now' : 'May 12, 2024'}</p></div><div className="flex gap-2"><button className="button button-secondary" onClick={()=>{navigator.clipboard?.writeText(key);setCopied(true);setTimeout(()=>setCopied(false),1500)}}>{copied?<Check size={14}/>:<Copy size={14}/>} {copied?'Copied':'Copy'}</button><button className="button button-ghost" aria-label="More actions"><MoreHorizontal size={17}/></button></div></div>)}</div></div></div> }
function Billing() { const [selected,setSelected]=useState('Pro'); return <div className="space-y-5"><div><p className="eyebrow">Workspace settings</p><h2 className="section-title">Billing & plans</h2><p className="mt-1 text-sm text-muted-foreground">Choose the plan that fits your voice AI usage.</p></div><div className="grid gap-4 lg:grid-cols-3">{[['Starter','$0','10K chars / month','For exploring the API'],['Pro','$49','100M chars / month','For production workloads'],['Scale','Custom','Unlimited usage','For growing teams']].map(([name,price,quota,desc],i)=><div className={`panel p-5 ${selected===name?'ring-2 ring-primary':''}`} key={name}><div className="flex items-center justify-between"><p className="font-medium">{name}</p>{i===1&&<Badge>Popular</Badge>}</div><p className="mt-6 text-3xl font-semibold">{price}<span className="text-sm font-normal text-muted-foreground">{i<2?'/ month':''}</span></p><p className="mt-2 text-sm text-muted-foreground">{quota}</p><p className="mt-5 border-t border-border pt-5 text-sm text-muted-foreground">{desc}</p><button className={`button mt-6 w-full ${selected===name?'button-primary':'button-secondary'}`} onClick={()=>setSelected(name)}>{selected===name?<><Check size={15}/> Current plan</>:'Select plan'}</button></div>)}</div><div className="panel overflow-hidden"><div className="border-b border-border px-5 py-4"><p className="font-medium">Invoice history</p></div>{['INV-2024-05','INV-2024-04','INV-2024-03'].map((x,i)=><div className="flex items-center gap-4 border-b border-border px-5 py-4 last:border-0" key={x}><div className="icon-box"><Wallet size={16}/></div><div className="flex-1"><p className="text-sm font-medium">{x}</p><p className="text-xs text-muted-foreground">{['May 1, 2024','April 1, 2024','March 1, 2024'][i]}</p></div><span className="text-sm font-medium">$49.00</span><button className="button button-ghost"><ArrowUpRight size={15}/><span className="sr-only">Open invoice</span></button></div>)}</div></div> }
function Docs() { const [lang,setLang]=useState('curl'); const code=lang==='curl'?`curl https://api.transvoice.ai/v1/transcribe \\\n  -H "Authorization: Bearer $TV_API_KEY" \\\n  -F file=@audio.mp3`:lang==='python'?`from transvoice import TransVoiceAI\n\nclient = TransVoiceAI(api_key=TV_API_KEY)\nresult = client.transcribe("audio.mp3")`: `const result = await transvoice.transcribe({\n  file: audioFile,\n  language: "en"\n})`; return <div className="space-y-5"><div><p className="eyebrow">Build with TransVoice</p><h2 className="section-title">Documentation</h2><p className="mt-1 text-sm text-muted-foreground">Everything you need to add voice intelligence to your product.</p></div><div className="grid gap-5 xl:grid-cols-[.8fr_1.4fr]"><div className="panel p-5"><p className="eyebrow">Quick start</p><div className="mt-5 space-y-1">{['Introduction','Authentication','Transcribe audio','Text to speech','Webhooks','Error handling'].map((x,i)=><button key={x} className={`flex w-full items-center justify-between rounded-lg px-3 py-2.5 text-left text-sm ${i===2?'bg-primary/10 font-medium text-primary':'text-muted-foreground hover:bg-muted hover:text-foreground'}`}>{x}{i===2&&<ChevronDown size={15}/>}</button>)}</div><div className="mt-8 rounded-xl bg-foreground p-4 text-background"><Code2 size={18}/><p className="mt-3 text-sm font-medium">Need help?</p><p className="mt-1 text-xs opacity-70">Our team is here to help you ship.</p><button className="mt-4 text-xs font-medium text-primary">Talk to an expert <ArrowUpRight size={13} className="inline"/></button></div></div><div className="panel overflow-hidden"><div className="border-b border-border p-5"><div className="flex items-center gap-2"><span className="rounded-md bg-emerald-500/10 px-2 py-1 font-mono text-xs font-medium text-emerald-600">POST</span><code className="font-mono text-sm">/v1/transcribe</code></div><p className="mt-3 text-sm text-muted-foreground">Transcribe an audio file into accurate, timestamped text.</p></div><div className="flex items-center justify-between border-b border-border px-5 py-3"><div className="flex gap-1">{['curl','python','javascript'].map(x=><button key={x} className={`rounded-md px-3 py-1.5 text-xs font-medium ${lang===x?'bg-muted':''}`} onClick={()=>setLang(x)}>{x}</button>)}</div><button className="button button-ghost" onClick={()=>navigator.clipboard?.writeText(code)}><Clipboard size={14}/> Copy</button></div><pre className="overflow-x-auto bg-muted/50 p-5 font-mono text-xs leading-6 text-foreground"><code>{code}</code></pre><div className="border-t border-border p-5"><p className="eyebrow">Example response</p><pre className="mt-3 overflow-x-auto rounded-lg bg-muted p-4 font-mono text-xs leading-5">{`{\n  "id": "tr_01h8...",\n  "text": "Welcome to TransVoice AI",\n  "duration": 2.48,\n  "language": "en"\n}`}</pre></div></div></div></div> }
export default function Page() { const [active,setActive]=useState('Overview'); const [dark,setDark]=useState(false); const [mobile,setMobile]=useState(false); const View=active==='Overview'?Overview:active==='API Keys'?Keys:active==='Billing & Plans'?Billing:Docs; return <div className={dark?'dark min-h-screen':'min-h-screen'}><div className="app-shell"><aside className={`sidebar ${mobile?'sidebar-open':''}`}><div className="flex items-center justify-between"><Logo/><button className="icon-button md:hidden" onClick={()=>setMobile(false)}><X size={18}/></button></div><nav className="mt-10 space-y-1">{nav.map(({label,icon:Icon})=><button key={label} onClick={()=>{setActive(label);setMobile(false)}} className={`nav-item ${active===label?'nav-active':''}`}><Icon size={17}/>{label}</button>)}</nav><div className="mt-auto space-y-1"><button className="nav-item"><LifeBuoy size={17}/>Help center</button><button className="nav-item"><Settings size={17}/>Settings</button><div className="my-4 border-t border-border"/><div className="flex items-center gap-3 px-3"><div className="grid size-8 place-items-center rounded-full bg-primary/15 text-xs font-semibold text-primary">JD</div><div className="min-w-0 flex-1"><p className="truncate text-sm font-medium">Jordan Davis</p><p className="truncate text-xs text-muted-foreground">jordan@acme.co</p></div><ChevronDown size={15} className="text-muted-foreground"/></div></div></aside>{mobile&&<button aria-label="Close menu" className="fixed inset-0 z-30 bg-black/20 md:hidden" onClick={()=>setMobile(false)}/>}<div className="main-wrap"><header className="topbar"><button className="icon-button md:hidden" onClick={()=>setMobile(true)}><Menu size={19}/></button><div className="flex items-center gap-2 text-sm text-muted-foreground"><span className="hidden sm:inline">Workspace</span><ChevronDown size={14}/></div><div className="ml-auto flex items-center gap-2"><button className="icon-button" onClick={()=>setDark(!dark)} aria-label="Toggle theme">{dark?<Sun size={17}/>:<Moon size={17}/>}</button><button className="avatar">JD</button></div></header><main className="content"><div className="mb-8 flex items-start justify-between gap-4"><div><p className="eyebrow">Tuesday, May 28, 2024</p><h1 className="page-title">{active==='Overview'?'Good morning, Jordan.':active}</h1><p className="mt-1 text-sm text-muted-foreground">{active==='Overview'?'Here’s what’s happening with your API today.':'Manage your TransVoice AI workspace.'}</p></div><div className="hidden items-center gap-2 sm:flex"><button className="button button-secondary"><Search size={15}/> Search</button><button className="button button-secondary"><Terminal size={15}/> API status <i className="status-dot"/></button></div></div><View/></main></div></div></div> }
